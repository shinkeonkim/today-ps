#include <iostream>
using namespace std;

int tc;
int a,b,c,d;
int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    cin >> tc;
    while(tc--) {
        cin >> a >> b >> c >> d;
        cout << b <<" "<< c <<" "<< c << "\n";
    }
}